# =============================================================================
# ELIS SLR Agent – Runtime dependencies (CI/CD)
# -----------------------------------------------------------------------------
# Scope
#   Packages required by harvest scripts and validation tools at runtime.
#   Developer tools (Ruff, Black, pytest) are installed by GitHub Actions
#   workflows separately and must not be listed here to avoid drift.
#
# Reproducibility
#   All versions pinned for deterministic behavior across runners and time.
#   Test any updates in CI before deploying to production workflows.
# =============================================================================

# JSON Schema validator
# - Used by: scripts/validate_json.py
# - Purpose: Validates harvested data against ELIS schema
# - Pinned for: Python 3.11 compatibility, reproducible validation
jsonschema==4.23.0

# HTTP client library
# - Used by: All harvest scripts (scopus, wos, ieee, crossref, etc.)
# - Purpose: API calls to academic databases with custom headers/auth
# - Pinned for: Stable connection handling, Python 3.11 compatibility
requests==2.31.0

# YAML configuration parser
# - Used by: All harvest scripts to read config/elis_search_queries.yml
# - Purpose: Load search queries, date ranges, and source configurations
# - Pinned for: Python 3.11 compatibility, consistent parsing behavior
PyYAML==6.0.2

# Apify API client (official Python SDK)
# - Used by: scripts/google_scholar_harvest.py
# - Purpose: Interact with Apify's Google Scholar scraper actor
# - Features: Automatic retries, dataset streaming, account management
# - Pinned for: API stability, reproducible Google Scholar searches
# - Docs: https://docs.apify.com/api/client/python
apify-client==2.4.1

# cURL bindings for Python
# - Used by: xploreapi/xploreapi.py (IEEE Xplore API client)
# - Purpose: HTTP requests to IEEE Xplore API via pycurl
# - Pinned for: Binary compatibility with Ubuntu CI runners
pycurl==7.45.4

# TLS certificate bundle
# - Used by: xploreapi/xploreapi.py (IEEE Xplore API client)
# - Purpose: Provides up-to-date CA certificates for HTTPS connections
certifi==2024.12.14
