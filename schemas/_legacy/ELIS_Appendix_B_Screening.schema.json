{   "$schema": "https://json-schema.org/draft/2020-12/schema",   "$id": "schemas/ELIS_Appendix_B_Screening.schema.json",   "title": "Appendix B — Screening Rows",   "type": "array",   "items": {     "type": "object",     "additionalProperties": false,     "properties": {       "id": { "type": "string", "minLength": 1 },       "source_id": { "type": "string", "minLength": 1, "description": "References A.id" },       "title": { "type": "string", "minLength": 1 },       "decision": { "type": "string", "enum": ["included", "excluded"] },       "reason": { "type": "string", "description": "Required when excluded" },       "decided_at": { "type": "string", "format": "date-time" }     },     "required": ["id", "source_id", "title", "decision", "decided_at"],     "allOf": [       {         "if": { "properties": { "decision": { "const": "excluded" } }, "required": ["decision"] },         "then": { "required": ["reason"] }       }     ]   } }
