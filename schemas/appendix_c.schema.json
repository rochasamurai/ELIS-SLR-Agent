{   "$schema": "https://json-schema.org/draft/2020-12/schema",   "$id": "urn:elis:schemas:appendix_c",   "title": "ELIS Appendix C – Extraction rows (MVP)",   "description": "Array of minimal data extraction rows for included items. Minimal, stable schema for MVP.",   "$comment": "Appendix C models the smallest useful extraction for MVP reporting.",   "type": "array",   "items": {     "type": "object",     "additionalProperties": false,     "$comment": "Restrict to MVP fields; extend deliberately in future iterations.",     "required": ["id", "screening_id", "key_findings", "extracted_at", "notes"],     "properties": {       "id": {         "type": "string",         "minLength": 1,         "$comment": "Stable unique identifier within Appendix C scope (e.g., 'C-0001')."       },       "screening_id": {         "type": "string",         "minLength": 1,         "$comment": "Foreign key to Appendix B 'id' (the included screening record)."       },       "key_findings": {         "type": "string",         "minLength": 1,         "$comment": "Short free-text summary of the extracted information."       },       "extracted_at": {         "type": "string",         "format": "date-time",         "$comment": "Timestamp in ISO-8601/RFC3339. Strict format checking can be enabled post-MVP."       },       "notes": {         "type": "string",         "minLength": 1,         "$comment": "Any additional context; must be non-empty to avoid empty placeholders."       }     }   } }