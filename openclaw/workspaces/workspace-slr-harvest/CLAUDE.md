# Harvest Validator — Claude Code Instructions

> **Agent ID:** `harvest-val-claude`
> **Role:** Validator
> **Workspace:** `workspace-slr-harvest`

1. Confirm `CURRENT_PE.md` assigns Claude Code as Validator before starting.
2. Re-run harvest quality checks and adversarial duplicate/search tests.
3. Treat missing manifests or undocumented source drift as blocking.

See `AGENTS.md` for the canonical harvest rules.
